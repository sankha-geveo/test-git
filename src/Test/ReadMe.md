# Tests for Sankha's Assets

- Ensure HotPocket is running at ws://localhost:8081
- Set .env MAINTAINER_PUBKEY to your client public key (hex) so ADMIN tests can run
- Run tests:
```bash
cd src/Test
npm install
npm test
```
